# try-cli (archived)

This C rewrite is **archived**. Use the original:

**https://github.com/tobi/try**

Same `try` CLI (Ruby, and a native Spinel binary). Arch Linux: `yay -S try-cli` will follow that repo.

The code here remains as a historical snapshot of a standalone C implementation (last release v1.5.3).
